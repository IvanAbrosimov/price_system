# Price Catalog - Веб-приложение прайс-листа

Современное веб-приложение для просмотра каталога электротехнической продукции с функцией корзины.

## 🚀 Быстрый старт

### Требования
- Docker и Docker Compose
- Node.js 20+ (для разработки)

### Запуск через Docker

```bash
cd price-catalog
docker-compose up
```

Приложение доступно: http://localhost:5173

### Запуск в режиме разработки

```bash
# Backend
cd price-catalog/server
npm install
npm run dev

# Frontend (в отдельном терминале)
cd price-catalog/client
npm install
npm run dev
```

## 📦 Структура проекта

```
price-catalog/
├── client/           # React + TypeScript + Vite
│   ├── src/
│   │   ├── components/   # UI компоненты
│   │   ├── hooks/        # React hooks
│   │   ├── utils/        # Утилиты
│   │   └── __tests__/    # Тесты
│   └── package.json
├── server/           # Node.js + Express + Drizzle ORM
│   ├── src/
│   │   ├── db/           # Схема PostgreSQL
│   │   ├── routes/       # API endpoints
│   │   ├── services/     # Бизнес-логика
│   │   └── __tests__/    # Тесты
│   └── package.json
└── docker-compose.yml
```

## 🔧 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/products` | Список товаров (с фильтрами) |
| GET | `/api/manufacturers` | Список производителей |
| GET | `/api/health` | Healthcheck |

### Параметры фильтрации

```
GET /api/products?manufacturer=Jung&search=розетка&page=1&limit=100
```

## 🧪 Тесты

```bash
# Backend тесты
cd price-catalog/server
npm test

# Frontend тесты
cd price-catalog/client
npm test

# Python тесты
cd ..
python3 -m pytest scripts/tests/ -v
```

**Покрытие: 93 теста**
- Backend: 35 тестов
- Frontend: 35 тестов
- Python: 23 теста

## 📝 Функционал

### Динамический расчёт сроков доставки

| Условие | Срок |
|---------|------|
| `qty_Астана >= заказ` | 6-10 дней |
| `qty_Астана + qty_Алматы >= заказ` | 10-14 дней |
| Иначе | по запросу |

### Корзина
- Хранение в localStorage
- Динамический пересчёт сроков при изменении количества
- Экспорт в заказ

## 🔄 Загрузка данных

Для обновления данных в PostgreSQL:

```bash
cd ..
python3 scripts/build.py --postgres
```

Опции:
- `--postgres` - только в PostgreSQL
- `--all` - в Google Sheets и PostgreSQL
- (без параметров) - только в Google Sheets

## 🌍 Деплой

### Railway

1. Создайте проект на Railway
2. Добавьте PostgreSQL
3. Подключите GitHub репозиторий
4. Настройте переменные окружения:
   - `DATABASE_URL` (автоматически от Railway)
   - `NODE_ENV=production`

## 📄 Лицензия

MIT
